Linux
1) install runtime wxgtk2.8
2) make
3) in case of errors uncomment line starting by "WX_LIBS" (see the makefile)
sudo cp libgarglk.so /usr/lib/

install:
libSDL_sound-1.0 (sudo pacman -S sdl_sound)


Windows